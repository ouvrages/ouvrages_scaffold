en:
  activerecord:
    models:
      <%= singular_table_name %>: <%= capitalize_first_letter(translated) %>

  <%= plural_table_name %>:
    index:
      title: List of <%= plural translated %>
      actions: Actions
      edit: Edit
      destroy: Delete
      confirm: <%= capitalize_first_letter(definite_article) %> <%= translated %> will by deleted. Are you sure?
      new: Add <%= indefinite_article %> <%= translated %>

    edit:
      title: Modify <%= indefinite_article %> <%= translated %>

    new:
      title: New <%= translated %>

    form:
      submit: Create <%= definite_article %> <%= translated %>
      cancel: Cancel

  navigation:
    <%= plural_table_name %>: <%= capitalize_first_letter(plural(translated)) %>
